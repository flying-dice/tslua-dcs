{
  "name": "@flying-dice/tslua-mizkit",
  "version": "0.1.0",
  "description": "DCS World Mission Kit, built with tslua-dcs and TypescriptToLua.",
  "keywords": [
    "lua",
    "http"
  ],
  "license": "ISC",
  "types": "./dist/index.d.ts",
  "main": "./dist/index",
  "scripts": {
    "build": "rimraf dist bundle && tstl -p tsconfig.tstl.json && tstl -p tsconfig.tstl-bundle.json",
    "build:watch": "tstl -p tsconfig.tstl.json --watch",
    "build:docs": "typedoc",
    "test": "jest --coverage"
  },
  "dependencies": {
    "@flying-dice/tslua-dcs-types": "0.1.0"
  },
  "devDependencies": {
    "@golevelup/ts-jest": "^0.4.0",
    "@types/jest": "^29.5.10",
    "jest": "^29.7.0",
    "lua-types": "^2.13.1",
    "ts-jest": "^29.1.1",
    "typescript": "^5.2.2",
    "typescript-to-lua": "^1.22.0"
  }
}
